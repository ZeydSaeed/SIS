# CONTENT-U01 Shared ops filter / form / status chips — CLOSED

- `OpsYearFilter` — server-side year filter
- `OpsFormField` / `OpsTextInput` — accessible form fields
- `StatusChip` + `dayOfWeekLabel`
- Arabic `status` / `days` / `applyFilter` catalog keys
