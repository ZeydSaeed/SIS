# LAUNCH — Reinstall Arabic desktop icon + open guest hub — CLOSED

- Ran `Install-SisDesktopShortcut.ps1` → سطح المكتب: `نظام معلومات الطالب.lnk`
- Launched `http://sis.test/hub?desktop=1` in Edge/Chrome `--app=` window mode
- Guest hub without login; school modules remain auth-gated
